<%= form_with(model: wheel) do |form| %>
  <% if wheel.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(wheel.errors.count, "error") %> prohibited this wheel from being saved:</h2>

      <ul>
        <% wheel.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :vehicle_id, style: "display: block" %>
    <%= form.text_field :vehicle_id %>
  </div>

  <div>
    <%= form.label :wheel_size, style: "display: block" %>
    <%= form.number_field :wheel_size %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
